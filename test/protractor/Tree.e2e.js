const TreePage = require('./tree-page');

describe('On Tree Page: ', () => {
    let page;

    beforeEach(() => {
        page = new TreePage();
        page.get();
    });

    // TODO - tests here
});